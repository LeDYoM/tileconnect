# Tile Connect project design

Architecture:
- Main executable that loads libraries as requested.
- TC main library that contains most of the domain classes to be used.

For dev_01:
- Dummy executable that loads the TC Main Controller.
- Library containg boards and Tokens.

